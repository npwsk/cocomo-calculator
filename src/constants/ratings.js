const ratings = {
  EXTRA_LOW: 'EXTRA_LOW',
  VERY_LOW: 'VERY_LOW',
  LOW: 'LOW',
  NOMINAL: 'NOMINAL',
  HIGH: 'HIGH',
  VERY_HIGH: 'VERY_HIGH',
  EXTRA_HIGH: 'EXTRA_HIGH',
};

const cdRatings = [
  ratings.EXTRA_LOW,
  ratings.LOW,
  ratings.NOMINAL,
  ratings.HIGH,
  ratings.VERY_HIGH,
  ratings.EXTRA_HIGH,
];

export { cdRatings };

export default ratings;
